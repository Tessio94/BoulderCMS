services:
  payload:
    image: payload-boulder
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      DATABASE_URL: postgres://postgres:dellas113@db-boulder:5432/payload_test
      PAYLOAD_SECRET: x/EEol7+ilIDCjPgI/G5kV5ggXEskKCHvNu0RBoervA=
    volumes:
      - payload-media:/app/media
    ports:
      - "3000:3000"
    depends_on:
      - db-boulder

  db-boulder:
    image: postgres:latest
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: dellas113
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql

volumes:
  pgdata:
  payload-media:
